#include "../inc/Error.hpp"
#include "../inc/Logger.hpp"

Error& Error::instance()
{
    std::lock_guard<std::mutex> guard(Error::constraction_mutex);
    static Error _instance;
    return _instance;
}

void Error::log(const std::string& text) const
{
    Logger::instance().log("ERROR : " + text);
}
