#include "../inc/Info.hpp"
#include "../inc/Logger.hpp"

Info& Info::instance()
{
    std::lock_guard<std::mutex> guard(Info::constraction_mutex);
    static Info _instance;
    return _instance;
}

void Info::log(const std::string& text) const
{
    Logger::instance().log("INFO : " + text);
}
